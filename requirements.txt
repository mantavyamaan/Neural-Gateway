# Core service
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0

# Routing engine math
numpy>=1.26.0

# Semantic domain classifier — fastembed uses ONNX runtime under the
# hood (no PyTorch dependency, ~30MB install vs ~800MB for sentence-
# transformers). gpt-4o-mini is the LLM tiebreaker for ambiguous prompts.
fastembed>=0.3.0
openai>=1.40.0

# Artifact inspection (optional but recommended — the router degrades
# gracefully if any of these are missing)
pymupdf>=1.24.0        # PDF inspection (import name: fitz)
Pillow>=10.3.0          # Image inspection
openpyxl>=3.1.2          # Spreadsheet inspection
python-pptx>=0.6.23       # Presentation inspection
mutagen>=1.47.0            # Audio metadata fallback (when ffprobe unavailable)

# Testing
pytest>=8.2.0
httpx>=0.27.0             # TestClient transport for FastAPI
